/* window.onload = function() {
  let primNavItems = document.getElementsByClassName("primary-menu-nav-item");
  for (var i = 0; i < primNavItems.length; i++) {
    primNavItems[i].addEventListener("mouseover", event => {
      document.getElementsByClassName('current-menu-item')[0].firstChild.classList.add("primary-menu-nav-link-transparent");
    });
    primNavItems[i].addEventListener("mouseout", event => {
      document.getElementsByClassName('current-menu-item')[0].firstChild.classList.remove("primary-menu-nav-link-transparent");
    });
  }
};
 */